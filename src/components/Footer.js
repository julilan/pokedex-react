import React from "react";

const Footer = () => {
  const style = {
    textAlign: "center",
    color: "black",
    padding: "1rem",
    minHeight: "5vh",
  };

  return (
    <footer style={style}>
      <small>a REACT23K project</small>
    </footer>
  );
};

export default Footer;
